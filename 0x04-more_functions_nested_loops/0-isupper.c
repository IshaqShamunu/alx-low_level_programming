#include "main.h"

/**
 * _isupper - Check the code and findout if the letter is upper
 * @x: Is the number to be checked
 * Return: 1 for upper 0 if not 
 */

int _isupper(int x);
{
	if (x >= 65 x &&  <= 90)
	{
	return (1);
	}
	return (0);
}
